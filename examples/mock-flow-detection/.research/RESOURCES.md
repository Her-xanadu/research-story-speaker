# Research Resources

> **MOCK** — 资源定位。路径失效时按 `git-linking.md` 恢复。

## Codebases

### flow-detector

**Purpose:** MOCK 主实验代码库 — 3-feature 与 full-feature 基线

**Git:** local toy repo, branch master, no remote

**Recovery source:** local-only

**Portability:** host-dependent

**Preferred relative location:** `../../../story-research-code`

**Last known local location:** (host-dependent; same as preferred relative) `../../../story-research-code`

**Remote location:** N/A

**Notes:** 模式 B 并列布局；`src/features.py` + `src/baseline.py` + `experiments/EXP-001/` + `results/EXP-001/`。入口与 EXPERIMENTS 一致：`experiments/EXP-001/run.py`；配置 `experiments/EXP-001/run_config.yaml`。

---

## Datasets

### CICIDS2017-MOCK-Subset

**Location:** `../../../story-research-code/data/mock_flows.csv`

**Read/write:** read-only (regenerated by run script)

**Notes:** MOCK 合成流记录（非真实 CICIDS 全量）

---

## Compute

### local-mac

**Use:** CPU experiments (MOCK)

**Access:** local shell

**Working directory:** `../../../story-research-code`

---

## External Capabilities

| Capability | Available via |
|------------|---------------|
| Literature | Web / local PDF |
| Independent reviewer | Codex / Claude / Cursor subagent |
| Git | native |
| Remote compute | N/A for MOCK |

---

_路径失效时按 git-linking.md 重新定位并更新本文件。_
