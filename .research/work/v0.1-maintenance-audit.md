# V0.1 Maintenance Audit (Gate 1+3 fix round)

Date: 2026-09-02  
Executor: Gate 1+3 Fixer，按 `framework-maintenance` 八项检查表对**当前磁盘文件**重跑。

> **作废声明：** 原 Worker I 审计的 Verdict「V0.1 ready to freeze」、以及「STORY as experiment log: PASS」「DISCOVERY vs EXPERIMENTS overlap: PASS」**全部作废**。那些判定与当时文件事实相反（STORY/STATE/DISCOVERY 互抄 F1）。本文件取代之。

## 范围与限制

- 已核对本轮允许修改的入口、八个状态文件、templates/references、adapters、EXP-001 reviews、本 work 文档。
- **未改** `.agents/skills/`、`.agents/subagents/`、`.agents/prompts/`、`.claude/`（并行修复者负责）。技能重叠项以并行修复完成后为准。
- Test F/G/J **未执行**。`.research/work/mock-test-scenarios.md` 只是 instruction-only 场景说明，不构成执行证据。
- 狗食阶段 subagent 输出**仅在会话内，未落盘**。`.research/work/` 无 `literature-scout` / `experiment-agent` / `result-analyst` 产物。
- Harness smoke 产物**待重跑**；拟存放 `.research/work/framework-dev/harness-smoke/`。当前该目录无落盘 stdout。

## Framework Maintenance Audit — 2026-09-02

### Checklist（八项）

| # | Check | Status | Notes |
|---|-------|--------|-------|
| 1 | Duplicate rules | PASS（本轮文件） | 循环图只在 `story-loop.md` §核心循环；Git 字段只在 `git-linking.md`；更新链只在 `state-files.md` §更新顺序。`AGENTS.md` / `README.md` / `state-files.md` 认知分层 / `experiment-record.md` Git 段改为一句链接。Skills 正文未在本轮终审。 |
| 2 | Skill overlap | NOTE | 入口 Skill Routing 仍为 10 个名字未变。并行修复者正在改 `SKILL.md`；本审计不对 skills 终态背书。 |
| 3 | AGENTS size | PASS | `wc -l AGENTS.md` = 66（< 150；目标 ≤ 100）。七段齐全。 |
| 4 | STATE size | PASS | `STATE.md` 45 行；Recently Completed 为指针，无 F1、无完整 hash。 |
| 5 | STORY as log | PASS | Evidence 仅机制结论 + EXP-001 指针；无 F1 / gap / n / commit。六段齐全。 |
| 6 | DISCOVERY vs EXPERIMENTS | PASS | Positive 为学习结论 + `Evidence: EXP-001`；数字只在 EXPERIMENTS。已恢复弱 IF 对照 Negative。 |
| 7 | Adapter drift | PASS（adapters） | `adapters/README.md` 已删除 F1/commit ground truth 与 Canonical read order；矩阵不再写 verified。`claude-code.md` 写清只自动读 CLAUDE.md + `.claude/skills/`。 |
| 8 | Unnecessary scripts | PASS | workspace 内无框架 `.py/.sh/.js/.ts`。 |

### Findings

- [MAJOR] Test F/G/J 未执行 — owner: Lead / 后续轮 — 不要用 `mock-test-scenarios.md` 冒充覆盖。
- [MAJOR] Harness smoke 无落盘产物 — owner: 后续 Worker G 重跑 — 存放 `.research/work/framework-dev/harness-smoke/`。
- [MAJOR] 狗食阶段 subagent 输出未落盘 — owner: 记录层 — `.research/work/<role>/` 空。
- [MAJOR] git tag / HEAD / index 一致性不在本轮处理 — owner: Lead 统一提交后再考虑重打 tag。
- [MINOR] Skills/prompts/subagents 并行修改未纳入本审计终态。

### Passed checks

- STORY 不再充当实验日志。
- DISCOVERY 不再复制 EXPERIMENTS Main Findings。
- EXPERIMENTS 有 Index + `Status: completed`，唯一绑定代码仓库 commit `b0621e2ed266cc26020fac5b3295a588469bb495`。
- AGENTS.md 短入口、七段、66 行。
- Adapter 不再复制科研数字与冷启动读序。
- 框架无脚本依赖。

### Release recommendation

**fix-first** — 不得宣称 V0.1 已可发布。本轮只修认知层与 adapter/状态 SoT；tag、smoke、Test F/G/J、skills 终态均未完成。

## §24 A–G（诚实判定）

| 条 | 判定 | 证据 |
|----|------|------|
| **A. Workspace 自包含** | **部分满足** | `.research/` + `.agents/` 可复制走。Mode B 代码在 `../story-research-code`，RESOURCES 主定位为相对路径；无 Git remote，换机后需询问用户。绝对路径仅留 Last known local location。 |
| **B. Story 可恢复** | **满足（文件层）** | 只读 AGENTS + PROJECT + STORY + STATE 可回答项目 / 当前 Story / 位置 / 下一步。STORY 约一页、六段、无性能数字。 |
| **C. History 可恢复** | **部分满足** | EXPERIMENTS / DISCOVERY / REVIEWS 能看出「先有三特征 Story，再跑 EXP-001」。弱 IF 对照已回 Negative。扣分：Test F/G/J 未执行，没有失败实验 / 推翻 Core Idea / 大量历史压缩的真实记录。 |
| **D. Experiment 可追溯** | **满足（当前文件 + 代码 HEAD）** | Codebase `flow-detector` → `../story-research-code` → commit `b0621e2ed266cc26020fac5b3295a588469bb495` → Entry `experiments/EXP-001/run.py` + Config `run_config.yaml` → `results/EXP-001/metrics.json` → `.research/reviews/EXP-001/`。`git cat-file -e` 与 `ls-tree` 三路径均存在。Status=`completed`。 |
| **E. Research Loop 可运行** | **满足（MOCK 文件层）** | 磁盘上存在 Story → EXP-001 → Discovery → Story/State 的一轮。狗食阶段 subagent 输出未落盘，不把 Phase 8 写成已有 work 产物。 |
| **F. 可迁移** | **部分满足** | `command -v`：codex / claude / dsh / cursor-agent **已安装**；opencode **未安装**。本机 smoke **无落盘产物**（待修复轮后重跑）。不以 verified 自称。OpenCode 未装则「至少四宿主+OpenCode」未满。 |
| **G. 易维护** | **部分满足** | Canonical Skill 根仍是 `.agents/skills/`；Claude 经 `.claude/skills/<name>` 目录 symlink。Adapter 已去科研 GT。扣分：skills 并行在改；smoke 未重跑；维护审计此前曾误标 PASS。 |

**Verdict: 不可冻结。** 原「ready to freeze」作废。
