You are running inside the research-story-speaker repository (workspace root). This is a MOCK ORCHESTRATION SMOKE TEST of the fixed multi-agent dispatch — NOT real science. Act as Main per AGENTS.md and use your NATIVE custom subagents (experiment-agent, literature-scout, result-analyst) defined for this harness. Do NOT role-play the roles yourself; actually spawn them as separate agent threads.

Run this exact flow:
1) Spawn the native experiment-agent (EXP-MOCK-01): in Python generate 200 samples from two 1-D Gaussians N(mean=0,std=1) and N(mean=3,std=1), 100 each, seed 42; classify by the midpoint threshold 1.5; report accuracy. Write the full report to examples/mock-flow-detection/.research/work/EXP-MOCK-01-run.md using the default work-file structure (## what was done / ## git commit -> "no code change" / ## result location / ## raw findings / ## issues). It must return ONLY the short two-tier decision summary (Task / Completion / Finding / Evidence pointer / Limitation / Recommended next / Proposed state changes).
2) In parallel, spawn the native literature-scout: no Obsidian vault is available, so immediately return NEEDS_REFRESH and write examples/mock-flow-detection/.research/work/LIT-MOCK-01.md (do not web-search).
3) Then spawn the native result-analyst in compact mode: it MUST open examples/mock-flow-detection/.research/work/EXP-MOCK-01-run.md from disk itself, interpret whether the accuracy is a reliable discriminating signal and name the best rival explanation, write examples/mock-flow-detection/.research/work/EXP-MOCK-01-analysis.md, and return ONLY the short summary.

STRICT BOUNDARIES: the subagents write ONLY under examples/mock-flow-detection/.research/work/. Do NOT edit ANY canonical .research file (PROJECT/STORY/STATE/DISCOVERY/EXPERIMENTS/LITERATURE/REVIEWS/RESOURCES) and do NOT touch the repo-root .research/ (it must stay UNINITIALIZED).

Then report back concisely: (a) the names/ids of the native subagent threads you actually spawned, proving they were separate agents and not you role-playing; (b) confirm each returned only a short summary while its full body went to its work file; (c) confirm result-analyst opened the executor report from disk; (d) confirm no canonical files were written and repo-root .research/ stayed UNINITIALIZED.
