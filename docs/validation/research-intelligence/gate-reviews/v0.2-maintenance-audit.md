# V0.2 Maintenance Audit (Gate E / Commit 9 evidence)

Date: 2026-09-04  
Mode: `framework-maintenance` **standard**  
Workspace: `/Users/herxanadu/research-story-speaker`  
Branch: `v0.2-research-intelligence`  
HEAD: `3291759` (`V0.2 Wave H: skill-evolution dogfood`)  
Executor: standard audit against `.agents/skills/framework-maintenance/SKILL.md` checklist **including V0.2 rows 10–12**.

> **This file is evidence for Gate E / Commit 9. It is not a Gate E Verdict.**  
> Gate 判定词汇（若后续 Gate E 使用）不得从本审计推出。本文件不打 tag、不 commit、不改 Skills。

## Scope

Read-only except this file. Contrasted against Protocol owners (`state-files.md`, `experiment-record.md`, `story-loop.md`, `git-linking.md`, `.agents/subagents/reviewer.md`) and Layer 2 under `.agents/references/research-intelligence/`. Sample state: root `.research/*.md` (bloat only). Adapters, `AGENTS.md`, `CLAUDE.md`, `README.md`, 12 Skills, 8 templates, `subagent-handoff.md`.

Not executed: harness smoke, regression fixtures, Gate E independent review.

## Framework Maintenance Audit — 2026-09-04

### Checklist（12 项）

| # | Check | Status | Notes / suggested fix |
|---|-------|--------|-----------------------|
| 1 | Duplicate rules | **PASS** | Outcome 完整表只在 `experiment-record.md` §Outcome 值。Verdict 完整表只在 `.agents/subagents/reviewer.md` §Verdict。Story 六段职责只在 `state-files.md` §STORY.md（`story-maintenance` 只列段名并链接）。更新链只在 `state-files.md` §更新顺序。Skills / prompts 均写 cite / do not copy。`research-loop` 的 Idea-gate / Evidence-gate 行是调度接线，未把 `story-loop.md` 循环图或 Outcome/Verdict 表抄进 Skill。 |
| 2 | Skill overlap | **PASS** | 12 个 Skill 各有 When-not-to-use。`idea-evaluation` 不写 STORY/DISCOVERY/EXPERIMENTS、不建 EXP；`evidence-verification` 不写 Outcome/Story、不替代 `result-analysis` / `experiment-review`；`research-loop` 只调度；`framework-maintenance` 不进日常科研 loop、不是第 13 个 Skill。 |
| 3 | AGENTS size | **PASS** | `wc -l AGENTS.md` = **78**（§尺寸建议 < 150）。七段齐全：Workspace Identity / Start Here / Autonomy / Research Memory / Skill Routing / Subagents / Maintenance。Skill Routing 12 名与磁盘目录一一对应。仍标 `Framework base: v0.1.1`（发版前预期，不在本行判 fail）。 |
| 4 | STATE size | **PASS** | `.research/STATE.md` = **33 行**（几十行目标）。`Story Status: NOT_INITIALIZED`；Active Experiment `none`；无实验数字、无 commit、无历史流水账。根目录无第二份 `STATE.md`。 |
| 5 | STORY as log | **PASS** | `.research/STORY.md` 固定六段，全部 `_Not established yet._`，约一页，无 EXP 引用、无性能数字。与 `STORY.template.md` 结构一致。 |
| 6 | DISCOVERY vs EXPERIMENTS | **PASS** | 根 DISCOVERY 七段均为 `_None yet._`；EXPERIMENTS 仅空 Index（含 Outcome 列，无 section）。无互抄。协议仍：`failed`+`not-assessed` 不进 Negative Discovery（`result-analysis` 引用 token，未抄表）。 |
| 7 | Adapter drift | **PASS** | `adapters/` + `CLAUDE.md` 只回答宿主入口 / Skills 发现 / Subagent / Reviewer / MCP。无科研逻辑、无 Outcome/Verdict 表、无冷启动读序副本（读序仍在 `AGENTS.md` Start Here）。`.claude/skills/` **12** 个目录 symlink → `../../.agents/skills/<同名>`，含 `idea-evaluation` 与 `evidence-verification`。OpenCode：documentation-only / not tested（Deviation allowed；与 v0.1.1 已知债务相同）。 |
| 8 | Unnecessary scripts | **PASS** | workspace 无框架 `.py` / `.sh` / `.js` / `.ts` / Makefile / shebang。`adapters/README.md` 仍写 instruction-only。`find` 在排除 `.git` / `graphify-out` / `docs` 后为空。 |
| 9 | Cross-file terminology | **PASS** | `.agents` 内 `七个` / `seven state` = 0。一律 **八个** canonical 状态文件。Status vs Outcome：Status 生命周期、Outcome 科学语义，表只在 `experiment-record.md`。Verdict 一律 `cite reviewer.md` / `Verdict: <per reviewer.md>`，模板不复述词表。Review 文件名 `*-review-r<N>.md`。八模板与 `state-files.md` / `experiment-record.md` 字段形状一致；`EXPERIMENTS.template.md` 写明不要把 Outcome 定义表抄进模板。 |
| 10 | Skill count is **12**, not 10 | **PASS** | `.agents/skills/` 恰 **12** 目录，无第 13 个：`workspace-resume`、`research-loop`、`research-memory`、`story-maintenance`、`idea-evaluation`、`literature-research`、`experiment-design`、`experiment-execution`、`result-analysis`、`evidence-verification`、`experiment-review`、`framework-maintenance`。含 `idea-evaluation` 与 `evidence-verification`。`AGENTS.md` 路由表与 `README.md` Canonical Skills（12）同名。`.claude/skills/` 12 symlink，无 fork。 |
| 11 | Intelligence refs are Layer 2, not Protocol | **PASS** | Layer 2 六件均声明 **Not Protocol**，不重定义 Outcome / Verdict / Story 六段。Idea-gate 四动作只在 `idea-and-mechanism.md` §H；§F 报告标签只在 `evidence-and-claim.md`（明确不是 Outcome）；§G 措辞强度词只在 `scientific-reasoning.md`（明确不是 Outcome/Verdict/Status）。Protocol 五件路径：`state-files.md`、`experiment-record.md`、`story-loop.md`、`git-linking.md`、`reviewer.md`（完整契约在 `.agents/subagents/reviewer.md`，与 V0.1 一致，不在 `.agents/references/`）。`story-loop.md` 仍是 Literature / Experiment / Review 三路，未把 Idea-gate / Evidence-gate 写进 Protocol（选择性门在 Skill + Layer 2）。 |
| 12 | Gates selective | **PASS** | `idea-evaluation` / `evidence-verification` When-to-use 为触发式，非常规 EXP 默认。`research-loop`：普通 exploratory = design → execution → result-analysis，**without** idea-evaluation、evidence-verification、reviewer、`result-analyst`。`literature-research` 默认 **light**；deep 仅 novelty / 新核心机制 / 冲突改 Story / 领域惯例瓶颈，并链接 `deep-literature-mode.md`。`scientific-reasoning.md` §F：exploratory 不自动触发三门。`experiment-review` 将 Evidence-gate 标为 high-stakes **optional prepend**。符合 `skill-evolution.md` §C–E 保护模式。 |

### Also verify（Skill 正文额外项）

| Item | Status | Evidence |
|------|--------|----------|
| `.agents/skills/` 是唯一 canonical Skill 根 | **PASS** | 无 `.codex/skills/`。`.claude/skills/` 仅为 symlink。 |
| 12 Skills，含 `idea-evaluation` 与 `evidence-verification` | **PASS** | 见 #10。 |
| Subagent handoff 使用 `.research/work/` | **PASS** | `subagent-handoff.md`：scout/lead/agent/analyst → `.research/work/<task-slug>.md`；reviewer → `.research/reviews/<EXP-ID>/`。八个 canonical 由 Main 更新。 |
| 无 Outcome / Verdict 表被复制 | **PASS** | `rg '\| Outcome \| 精确定义'` 仅 `experiment-record.md`。Skills/prompts/Layer 2/adapters **无** Verdict 枚举表。Layer 2 另有报告标签与措辞词表，均声明非 Protocol。 |
| `AGENTS.md` 仍合理小 | **PASS** | 78 行 / 七段。 |
| `STATE.md` 仍 UNINITIALIZED tiny | **PASS** | 33 行；`PROJECT.md` **Project Status: UNINITIALIZED**。 |
| 无额外 canonical `.research` 文件 | **PASS** | 仅八个 `*.md`。另有 `.research/work/.gitkeep` 与 `.research/reviews/.gitkeep`（非 canonical 状态文件）。无 `HYPOTHESES.md` / `RUBRIC.md` / `CLAIMS.md`。 |
| 无框架 `.py` / `.sh` | **PASS** | 见 #8。 |

### Findings

- [MINOR] `skill-evolution.md` 仍写 “V0.1.1 has 10 … V0.2 will add exactly 2 later”（约 L11–12；L239 附近同旨）— owner: `.agents/references/research-intelligence/skill-evolution.md` — suggested fix: Gate E / tag 之后改成现在时（V0.2 **added** exactly those two）。**本审计不改该文件。** 不导致 #10/#11 fail：磁盘 Skill 数已是 12，且该文件仍禁止 Protocol fork。

无 **CRITICAL**。无 **MAJOR**。

### Passed checks

- Outcome 表未复制到 Skills、prompts、Layer 2、adapters、templates。
- Verdict 表只在 `reviewer.md`；他处仅 cite。
- `AGENTS.md` 78 行，仍短入口。
- 根 `.research/STATE.md` UNINITIALIZED、33 行。
- 八个 canonical 状态文件，无第九个。
- 无框架脚本。
- Skill 计数 12，名字含 `idea-evaluation` 与 `evidence-verification`。
- Layer 2 不重定义 Outcome / Verdict / Story。
- Idea-gate / Evidence-gate / deep literature 均非每 EXP 默认。
- `.claude/skills/` 12 个 symlink，无内容 fork。

### Confirm（用户点名项）

1. **no Outcome/Verdict tables duplicated** — confirmed.  
2. **AGENTS.md still reasonably small** — confirmed (78 lines).  
3. **STATE.md still UNINITIALIZED tiny** — confirmed (33 lines, `NOT_INITIALIZED`).  
4. **no extra canonical .research files** — confirmed (eight only).  
5. **no framework .py/.sh in workspace** — confirmed.  
6. **Skill count 12 named including idea-evaluation and evidence-verification** — confirmed.

### Release recommendation

**freeze**（就本 standard 审计的框架卫生而言：12 行 checklist 无 fail，无 CRITICAL/MAJOR。）

这 **不是** Gate E Verdict，也 **不是** 打 `v0.2` tag 的授权。发版/Verdict 由独立 Gate E 审核决定。MINOR（`skill-evolution.md` 时态）可在 tag 后由 Lead 指派，不阻塞本审计的 freeze 卫生结论。

已知债务（沿用，非本审计新发现）：OpenCode 未实测；README 仍列 Test F/G/J 等 V0.2 项。未跟踪 `graphify-out/` 不是 canonical 科研或框架文件，忽略。
