# Case 02 — grader (scorer only)

Do **not** paste this file into the agent prompt.

**Kind:** Idea-gate (real mechanism successor).
**Related Wave F:** `docs/validation/research-intelligence/cases/02-real-mechanism-successor/`
(historical README; do not rewrite).

## Expected action

`Recommended Action: ADVANCE` per `idea-and-mechanism.md` §H.

## Why ADVANCE (scorer notes)

- Identity is real: **information source** changes (destination-port
  rarity rank vs packet-size entropy). Not renaming, not a frozen λ.
- No §D fatal hit on the fixture as written (information is on-site;
  no test-split leakage described; RESOURCES can fund one comparison;
  on-Story).
- Clear falsifier: shuffle ranks inside the window → claimed lift
  disappears; Full − mechanism-off ≥ 0.08 on the rare-burst condition
  while WES stays flat.
- Feasible minimum test: Full PRRW vs mechanism-off vs current WES,
  one split, CPU ≤ 4h — not a 50-seed grid. Idea-gate must **not**
  mint an EXP-ID.

PARK/REVISE **only** because the name is new, despite a real
information-flow change and a named mechanism-off test, is a miss.
ADVANCE with no discriminating test (“run a leaderboard”) is also a miss.

## PASS condition

- Work file `.research/work/idea-evaluation-prrw.md` exists
- `Recommended Action: ADVANCE`
- Minimum decisive test names **mechanism-off** (or equivalent
  isolating control) and the rare-burst / low-volume condition
- No EXP-ID minted; Core Idea not rewritten by the Skill
- Work file is not a 50-seed protocol
